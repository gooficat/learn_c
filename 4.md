### Beginning to Code

In this chapter we will begin to write C code.

First things first, we will use the terminal to create a folder to store our code. You could always do this with the file manager but this is a good learning opportunity.

First, open your terminal.

## IMPORTANT NOTICE
If you are on Windows, you should be using the CLANG64 shell, not the Command Prompt or Powershell. 

In case you were wondering, shell is the program that interprets the commands that you type into the terminal.

---

Then, use the ``cd`` command to navigate to your user folder

``cd ~``
Press Enter to execute a command

Choose a place to create our folder. Then, navigate to it. You can use relative navigation. For example, to enter the Documents folder, type 

``cd Documents``

To navigate upwards out of a folder you can type

``cd ..``

I like to create a folder in the user folder called ``Projects``

use the ``mkdir`` command to create folders (also known as directories)

``mkdir Projects``

Then we can make a subdirectory called ``Learning_C``, for example

Either navigate into the folder using ``cd Projects``, and create it from there, or type:

``mkdir Projects/Learning_C``

to create it from outside.

Now, we can enter that folder, and create one more folder. This one can contain our code for this chapter.

``mkdir Hello``
``cd Hello``

Now, if you're using VSCode, you can just open it and press "open folder", and select our "Hello" folder. You can then open an integrated terminal window by pressing ``ctrl + ` `` or ``ctrl + ~``, depending on the version.

We will need to create a file. We can call it ``main.c`` to indicate that it contains our entry point, ``main``. 

On VSCode you can create a file by clicking the button just above the file browser, or pressing `ctrl + n` and then `ctrl + s`.
If you aren't using VSCode, just type ``touch main.c``


Then, open the file. On VSCode this is just done by double-clicking on it. For Helix, we type ``hx main.c``

Helix has multiple *modes*. To enter "insert" mode, whch is the mode we use for editing the file, press `i`. To exit insert mode, press `esc`. To close the file, leave insert mode and type ``:q``, and press enter. To forcibly close without saving, type ``:q!``. To save, type ``:w``. To save and close, type ``:wq``.

Type the following code.

```c
extern int puts(char const *);

int main(void) {
	puts("Hello, World!");
	return 0;
}

```


Then, save the file, and then open a terminal. If you are using Helix, just save and close the file.

Then, type the following command

``clang main.c -o hello``

This will optimize, compile, assemble, and link your file all in one command.

You can type

``ls``

to view the contents of this folder now

You should see a file called ``hello`` or ``hello.exe``. Type

``./hello``

To run the file.

You should see the message 

``Hello, World!``

You could also compile the code in steps.

``clang -S main.c -o main.asm``
``clang -c main.asm -o main.obj``
``clang main.obj -o main``


Let's break down the code we wrote.

```c
extern int puts(char const *);

int main(void) {
	puts("Hello, World!");
	return 0;
}
```

The first line is an external symbol declaration. In this case, it's a function. This function returns an integer, and takes in a pointer to a character, declared constant, to say that the function does not edit the code at the pointer (This is more of a promise than a rule).

But wait... What is a function, anyway?

## What is a function? 

---

A function is an isolated piece of code that can be called. It can take in parameters and output a single value.

A typical function looks like this in Assembly:

```s
my_function:
	push rbp
	mov rbp, rsp
	# [Some code here...]
	pop rbp
	ret
```

You're probably wondering what `rbp` and `rsp` are, and what `push` and `pop` do.

RSP is a special register that *points* to (contains the address of) the top of the stack. The stack is a block of memory given to each program that "grows", with the top of it moving "up" every time you push a value onto it. In reality, it moves toward the lower addresses, i.e. from 0x82000000 to 0x81600000

You can imagine it as a stack of papers. You "push" some paper by placing it on the top of the stack. Then you can "pop" one off by removing it from the top. However, unlike a stack of papers, you can modify and read values from lower parts of the stack.

If you exceed the memory size of the stack by pushing too many values, you get a stack overflow! If you try to pop a value off an empty stack, you get a stack underflow. You may have heard these terms before.

A useful purpose for the stack is for functions. We can create a "stack frame" which is an area of the stack that contains our function's variables for this specific call.

Instead of saying "my variables are at this location in memory", we say "I will claim this amount of bytes starting from wherever the stack pointer currently is, and store my variables there". The benefit of using the stack for variables is that a function can be called inside of itself, or inside of a function that it itself called. This is called "recursion". 

To avoid convoluted code, we also typically use a "base pointer", almost always RBP. This points to the base of our stack frame, meaning its location before we begin pushing and popping values to save them between operations (i.e. when we don't have enough registers to hold it). This means we can always use the same offset relative to rbp, something like ``[rbp - 4]``

When you use the "call" instruction, the instruction pointer is pushed onto the stack, and an unconditional jump is executed. When you use a "ret" (return) instruction, you are popping the value off the stack, back onto the instruction pointer. (Because of this, we must always restore the stack to where it started when we finish a function).

---

The C language is made up entirely of declarations. You are always declaring a variable, function, or label.

Variables in C can have different linkage, storage, base types, and various other flags.

*Linkage* refers to whether a variable is global, static, or local.

A global variable is visible to outside files, as we saw in the previous chapter. In C, we call this *extern linkage*.

A static variable is one visible *only* inside of this file, but throughout the whole file.

A local variable is one that is only visible inside its *scope*. This would be anywhere that is surrounded by curly braces.

Variables not contained in a scope have extern linkage by default. Inside functions, they are local.

*Storage* refers to how the variable is stored in memory. The 3 options are:

- `static`, meaning it exists as a label followed by a number of bytes to contain your variable

- `auto`, meaning it is stored on the stack. It is *automatically* freed upon exiting the scope by restoring the stack pointer.

- `register`, meaning it is stored on a register. Nowadays this one is deprecated, and instead `auto` will put the variable on a register if there is one available.

- `extern`, meaning it comes from somewhere else. This can be outside the file or later on in the file. Either way, it's a forward declaration.

- `inline`, meaning it doesn't exist in the code but a copy is pasted wherever the variable is referenced. This only applies to functions.

- `typedef` is grammatically a storage class, but really is just a way to declare alternative names for a variable type.

Variables not contained in a scope are statically stored by default, and inside functions they are auto by default. If you try to call an undefined function, it is declared extern automatically, but this is limited and not recommended.

As for the actual *type* of the variable, it can be from these base categories:

- `int`. This is a variable that just contains a whole number, which is what we've been working with all this time.

- `float`. This is a special number that holds a rough estimate of a number that may contain decimal parts, like `3.141`.

- `char`. This is a special integer that is one byte long and holds a text character.

- `void`. This is a type that has a width of zero! It's used for saying a function doesn't return anything at all (so doesn't need a register or space on the stack to place its return value), and also used for pointers to unknown values that can be freely cast to other pointer types. Inside of the C language, it is considered an *incomplete type*, meaning it doesn't have all of the necessary attributes defined. It has undefined width, for example, and no defined category, which is why it can be freely cast.

A "pointer" is an address of a variable. You can give an underlying type to a pointer, so as to say "this variable points to another variable which is an integer", for example.

"Casting" is the behaviour of converting between types. Sometimes it is implicit, like passing a char into a function with a parameter that expects an int, and sometimes we do it manually.

On older C versions, variables could be int by default without specifying. Now, it's a bit more explicit and you can't omit all type specifiers.

There are variants of the types we mentioned above

- `long int` is a larger integer, and `long long int` is usually larger still. You can omit the word `int` here and just say `long` or `long long`.

- `short` is usually a shorter int.

- `double` is a double-precision floating point number, and `long double` is at least as precise, sometimes moreso but it depends on the platform.

Note that these are very vague. This is because C is a multi-platform language designed to make it easy to port code to other architectures, so the type specifiers are essentially just hinting.

We can also specify if an integer is `signed` or `unsigned`, which we spoke about earlier. As we said, a signed integer can hold negative and positive values, but an unsigned integer is always positive. This is another case where the word `int` can be omitted.

`char` can also act as an integer but we have to specify the signed-ness because char can have unique behaviour on some platforms.

For our purposes, since we're working with a 64-bit x86_64 system compiled with Clang, our type sizes are:

`char` is 1 byte (8 bits)
`short` is 2 bytes (16 bits)
`int` is 4 bytes (32 bits)
`long` and `long long` are both 8 bytes (64 bits)

All pointers are 8 bytes (64 bits)

However, as you'll see later, we have a more verbose way to specify the size of an integer, through typedefs that come with the C language via a `header file`. We will touch on header files later.

There are other attributes that can be added to a variable, with *qualifiers*, such as:

- `const`. This hints / promises to the compiler that we never change the value of this variable. If we try to do so directly we will get a complaint, usually an error.
- `volatile`. This means that we want it to stay in one place in memory, and have all references to it reference it directly.

There are numerous others also but these are the most relevant.

The next part is the declarator. This includes the name of the variable, and parts that may come before and after it.

- `my_variable`, a plain variable
- `*my_variable`, a pointer
- `my_variable[123]`, an array. This is a block of memory containing more than one instance of a type in a row.

We can add multiple of these to a single declaration.

``my_array[4][32][12]``

``***my_pointer``

``*ptr_to_array[123]``

A variable declaration takes the following form:

[storage] [type] [qualifiers] [declarator]

It's worth noting that if we declare a pointer:

``char * my_pointer;``

We can add qualifiers to the pointer and underlying type separately.

``char const * const my_pointer``

It always applies to whatever declarator is nearest to it.

``char const * my_pointer`` means a pointer to a constant character, whereas
``char * const my_pointer`` means a constant pointer to a character, i.e. you can't modify the variable holding the address.

So we now know that
```c
extern int puts(char const *);
```
is a declaration of an external function that returns a signed integer, and takes in an address pointing to a character (in this case, the beginning of a null-terminated string), which we have declared constant, meaning we can pass constant variables into this function.

Please note that extern can be omitted for forward declarations of functions, like so:
```c
int puts(char const *);
```


The next lines declare the `main` function, and define it. This one takes in nothing, indicated by `void` being placed in the parameter list. It returns a signed integer.

The function's content is defined too. It is a function call.
As you can see, it consists of a symbol followed by parentheses, with a string inside of it. Strings in C can be declared anywhere, and they are pooled together later on, usually in the `rodata` section of the executable.

Now, to fully understand the components of this function call, we need to discuss what an *expression* is, and what a *statement* is.

An expression is something that evaluates to a value, even if that value is `void`.

A statement is something made up of one or more expressions, which evaluates all of its output values in some way.

That's a weird way to put it, but here's how I like to think of it.

Pretend we have no registers to store values, only a stack.

An expression pushes a value onto the stack when it finishes.

A statement returns the stack to however it was before executing.

These are expressions. Think of their values as being pushed onto the stack.

``MyVariable``

``main()``

``0.5``


C also supports arithmetic operations. These are like special intrinsic function calls, but with operator priority. You probably have learned operator priority in school, such as with the acronym **PEMDAS**. Parentheses, exponents, multiplication / division, addition/subtraction (or **BODMAS** which says "braces" instead of "parentheses", and reorders D and M).

``my_variable = 123 + my_variable``

The `=` operator in C assigns values, it doesn't compare them!

In C, we have extra operators. These are used for bitwise arithmetic and logical operation.

We have bitwise arithmetic which operates on the bits instead of numbers. For example:

- ``|`` is the ``OR`` operation. For each bit, if either operand sets it as 1, the output version becomes 1.
- ``&`` is the ``AND`` operation. For each bit, if both operands have it set as 1, the output version becomes 1.

For example, `4 & 106`:

00000100 & 01101010 = 01101110

Logical operators are things like "are these 2 variables equal?" or "Is this number larger than my variable?"

